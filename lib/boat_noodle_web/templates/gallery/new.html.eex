<h2>New Gallery</h2>

<%= render "form.html", Map.put(assigns, :action, gallery_path(@conn, :create, BN.get_domain(@conn))) %>

<span><%= link "Back", to: gallery_path(@conn, :index, BN.get_domain(@conn)) %></span>
