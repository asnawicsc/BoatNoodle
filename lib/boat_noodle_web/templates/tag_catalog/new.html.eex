<h2>New Tag catalog</h2>

<%= render "form.html", Map.put(assigns, :action, tag_catalog_path(@conn, :create, BN.get_domain(@conn))) %>

<span><%= link "Back", to: tag_catalog_path(@conn, :index, BN.get_domain(@conn)) %></span>
