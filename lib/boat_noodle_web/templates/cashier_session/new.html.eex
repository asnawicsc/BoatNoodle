<h2>New Cashier session</h2>

<%= render "form.html", Map.put(assigns, :action, cashier_session_path(@conn, :create, BN.get_domain(@conn))) %>

<span><%= link "Back", to: cashier_session_path(@conn, :index, BN.get_domain(@conn)) %></span>
