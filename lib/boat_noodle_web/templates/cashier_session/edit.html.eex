<h2>Edit Cashier session</h2>

<%= render "form.html", Map.put(assigns, :action, cashier_session_path(@conn, :update, @cashier_session)) %>

<span><%= link "Back", to: cashier_session_path(@conn, :index) %></span>
