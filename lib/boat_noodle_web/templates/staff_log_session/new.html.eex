<h2>New Staff log session</h2>

<%= render "form.html", Map.put(assigns, :action, staff_log_session_path(@conn, :create)) %>

<span><%= link "Back", to: staff_log_session_path(@conn, :index) %></span>
