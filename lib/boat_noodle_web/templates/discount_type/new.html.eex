<h2>New Discount type</h2>

<%= render "form.html", Map.put(assigns, :action, discount_type_path(@conn, :create, BN.get_domain(@conn))) %>

<span><%= link "Back", to: discount_type_path(@conn, :index, BN.get_domain(@conn)) %></span>
