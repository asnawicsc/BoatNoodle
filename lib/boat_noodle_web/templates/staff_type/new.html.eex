<h2>New Staff type</h2>

<%= render "form.html", Map.put(assigns, :action, staff_type_path(@conn, :create, BN.get_domain(@conn))) %>

<span><%= link "Back", to: staff_type_path(@conn, :index, BN.get_domain(@conn)) %></span>
